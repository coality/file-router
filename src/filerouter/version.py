"""Application and data-schema versions."""

from __future__ import annotations

__version__ = "1.0.0"

# Version of the metadata schema produced by this application.
METADATA_SCHEMA_VERSION = "1.0"

# Metadata schema versions this application can read (forward tolerance).
SUPPORTED_METADATA_SCHEMA_VERSIONS = ("1.0",)
